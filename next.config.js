/** @type {import('next').NextConfig} */
const nextConfig = {
  // Note: build uses --turbopack flag (workaround for Node.js v24 + Windows EISDIR bug)
  // See package.json scripts
}

module.exports = nextConfig
